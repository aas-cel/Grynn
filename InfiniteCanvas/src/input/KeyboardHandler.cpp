#include "KeyboardHandler.hpp"

KeyboardHandler::KeyboardHandler(InputManager& inputManager) : inputManager(inputManager) {}

void KeyboardHandler::handleEvent(StyleCPP::KeyEvent* event) {
    // TODO: Implement keyboard shortcuts for tool switching, undo/redo, etc.
}
