#include "GestureHandler.hpp"

GestureHandler::GestureHandler(InputManager& inputManager) : inputManager(inputManager) {}

void GestureHandler::handleEvent(StyleCPP::GestureEvent* event) {
    // TODO: Implement gesture handling (pinch, rotate, etc.)
}
